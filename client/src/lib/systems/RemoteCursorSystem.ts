/**
 * Remote Cursor System
 */
export const RemoteCursorSystem = {
    handleMouseMove(x: number, y: number) {
        // Send cursor position to opponent
    },

    updateOpponentCursor(x: number, y: number) {
        // Update opponent's cursor UI
    }
};
